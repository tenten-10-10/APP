# プライバシーポリシー（ProjectStock）

最終更新日: 2026-06-25
（サポートURL・問い合わせ先は配布時に差し替えてください: `https://example.com/projectstock`）

ProjectStock（以下「本アプリ」）は、利用者のプライバシーを尊重し、収集する情報を
最小限に保つよう設計されています。本アプリは外部の運営者サーバーを持たず、独自の
ログインや会員登録を必要としません。

## 1. 収集・保存する情報

本アプリが扱うデータ（プロジェクト、フォルダ、製品、保管場所、在庫イベント、
QRラベル、製品写真など）は、次のいずれかに保存されます。

- **お使いの端末内**（オフライン時もすべて閲覧・編集できます）
- **お使いの iCloud アカウント**（Apple の CloudKit。プライベートデータベース、
  および共有した場合は共有データベース）

これらのデータは **本アプリの開発者のサーバーには送信されません**。iCloud 上の
データの取り扱いは Apple のプライバシーポリシーに従います。

## 2. カメラ

QRコードの読み取りにのみカメラを使用します。**カメラ映像を保存・送信することは
ありません。** カメラの利用を拒否しても、在庫の閲覧・編集や QR ラベルの書き出しなど、
スキャン以外の機能は利用できます。

## 3. 写真

製品に写真を添付する場合のみ、写真ライブラリへアクセスします。選択した写真は
縮小・圧縮のうえ、上記のデータと同じく端末内および iCloud に保存されます。

## 4. QRコードの内容

本アプリが生成する QR コードには、**商品名・数量・価格・利用者名・CloudKit Record ID
などは一切含まれません。** 短い不透明な識別子（`IQ` で始まる18文字）のみが格納され、
その識別子と対象物の対応は端末内 / iCloud のデータベースでのみ解決されます。

## 5. 端末識別子

在庫イベントの操作端末を区別するため、本アプリは**アプリ内で生成した UUID** を
端末識別子として使用します。**広告識別子（IDFA）等は使用しません。** この UUID は
端末内（必要に応じて Keychain）に保存され、外部送信されません。

## 6. 解析・広告

本アプリは**第三者の解析 SDK・広告 SDK・トラッキングを一切使用していません。**
利用状況の収集やプロファイリングも行いません。

## 7. iCloud 共有

プロジェクトを iCloud 共有した場合、招待した相手は当該プロジェクトのデータを
権限（閲覧のみ / 編集可）に応じて参照・編集できます。共有は CloudKit の仕組みを
利用し、共有相手の Apple ID とのみ行われます。

## 8. データの書き出し

利用者の操作で在庫データを JSON 等に書き出せます。書き出したファイルは一時領域に
作成され、共有後に一定時間で自動削除されます。診断ログには製品名やメモを含めません。

## 9. データの削除

プロジェクトや製品を削除すると、関連データは端末および iCloud から削除されます
（同期反映には時間がかかる場合があります）。アプリの削除や iCloud からのサインアウトに
よってもローカルデータは失われます。重要なデータは事前に書き出してください。

## 10. お問い合わせ

本ポリシーに関するお問い合わせ先は、配布時に以下へ差し替えてください。

- サポート: `https://example.com/projectstock/support`
- 連絡先: `support@example.com`
